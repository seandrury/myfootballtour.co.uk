{% extends "_layouts/main" %}

{% block content %}

    <div class="container py-large">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <h1 class="h2 mb-5">{{ entry.heading }}</h1>

                {{ entry.body }}
    
                <div class="py-5">
                    {% include "_svg/england-and-wales.svg" %}
                </div>
            </div>
        </div>
    </div>

{% endblock %}